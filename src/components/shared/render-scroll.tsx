import React from 'react'

export const renderScrollThumb = (props: DivProps) => (
  <div className={'scroll-thumb'} {...props} />
)
export const renderScrollTrack = (props: DivProps) => (
  <div className={'scroll-track'} {...props} />
)
