import React from 'react'

export const renderScrollThumb = (props: HTMLProps.div) => (
  <div className={'scroll-thumb'} {...props} />
)
export const renderScrollTrack = (props: HTMLProps.div) => (
  <div className={'scroll-track'} {...props} />
)
